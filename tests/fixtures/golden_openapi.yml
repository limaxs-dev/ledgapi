components:
  securitySchemes:
    bearerAuth:
      scheme: bearer
      type: http
info:
  title: Golden
  version: 1.0.0
openapi: 3.0.3
paths:
  /users/{id}:
    get:
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                type: object
          description: Successful response
      security:
      - bearer: []
      summary: Get user
